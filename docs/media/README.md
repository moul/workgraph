# Media

`web.png` — screenshot of the read-only web dashboard (`workgraph ui --serve`).
Capture it with any browser against the example workspace:

    workgraph -C examples/basic ui --serve
    # open http://localhost:8081 and screenshot into docs/media/web.png

The README references `docs/media/web.png`.
